enum ViewState {
  busy,
  done,
  error,
  none,
  noInternet,
}

enum MediaType {
  video,
  news,
}
enum AuthType {
  x,
  facebook,
  google,
  email,
}

enum PostListOrderType {
  latest,
  trending,
}
